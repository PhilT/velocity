NOTES

* Add _id to fields that have a collection

show_condition - shows the text version if condition true
edit_condition - shows the input field version if condition true
if both are false it's not shown

Show
  show all fields except first completed_on check_box (:show => :input)

