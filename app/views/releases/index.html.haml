- @releases.each do |release|
  %h3
    %span
      = "Tasks released #{release.finished_at.to_s} - Velocity: #{display_velocity(release.velocity)}"
  %div#releases= render :partial => 'tasks/tasks', :object => release.tasks

