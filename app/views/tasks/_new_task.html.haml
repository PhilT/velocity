%li.task{:class => "task_new"}
  - form_for new_task, :builder => CustomFormBuilder do |f|
    %ul.info
      %li
        = f.select :category_id, :collection => @categories
        = f.select :when_id, :collection => @whens
        = f.text :name

