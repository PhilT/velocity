%ul.tasks{:id => type}
  %li.heading
    %h3= heading +" (#{tasks.size})"
    %span.message
    - tasks.each do |task|
      = render :partial => task.new_record? ? 'new_task' : 'task', :object => task, :locals => {:field => nil}

