%ul.tasks{:id => type}
  %li.task.heading
    %h3= heading +" (#{tasks.count})"
    %span.message
    - tasks.each do |task|
      = render :partial => task.new_record? ? 'new_task' : 'task', :object => task, :locals => {:field => nil}

