= render :partial => 'tasks', :object => @now_tasks, :locals => {:heading => 'Tasks for the current release', :id => 'now_tasks'}
= render :partial => 'tasks', :object => @other_tasks, :locals => {:heading => 'Tasks for a future release', :id => 'later_tasks'}

= render :partial => 'tasks', :object => @new_tasks, :locals => {:heading => 'Add a new task', :type => 'new_task', :id => 'new_tasks'}

